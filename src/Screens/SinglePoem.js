//dependencies
import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const SinglePoem = () => {
  return (
    <View style={{backgroundColor: '#ffffff', flex: 1}}>
      <Text>Single Poem Component</Text>
    </View>
  );
};

export default SinglePoem;

const styles = StyleSheet.create({});
